name: CI

on:
  push:
    branches:
      - master
      - "feature/**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Show project files
        uses: ls -la 

      - name: Show node version
        uses: node --version